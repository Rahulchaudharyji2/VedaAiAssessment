{
  "name": "vedaai-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@vedaai/web",
    "build": "npm run build --workspace=@vedaai/web",
    "start": "npm run start --workspace=@vedaai/web"
  }
}
